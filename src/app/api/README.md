# API Namespace

This namespace is reserved for future commerce work.

`/api/orders` is unbuilt.
`/api/payments` is unbuilt.

No route handlers are present here. The browser cart remains client-only.

Future commerce work must treat money fields as integer minor units with an explicit currency field.
